defmodule EnterpriseConnectorElixirSdkTest do
  use ExUnit.Case
  doctest EnterpriseConnectorElixirSdk

  test "greets the world" do
    assert EnterpriseConnectorElixirSdk.hello() == :world
  end
end
